node_modules/

# app.js é gerado pelo build (npm run build) a partir de src/main.js.
# Deixado no disco para abrir o index.html localmente sem servidor,
# mas não é versionado — src/ é a única fonte da verdade.
app.js
