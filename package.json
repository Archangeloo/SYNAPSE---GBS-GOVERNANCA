{
  "name": "synapse-gbs",
  "version": "1.0.0",
  "description": "SYNAPSE · CoE Projetos e Automações — dashboard de governança GBS",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --outfile=app.js --minify-whitespace",
    "dev": "esbuild src/main.js --bundle --format=iife --outfile=app.js --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^2.0.0"
  }
}
